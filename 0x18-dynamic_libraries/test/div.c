/**
* div - divides two integers and returns result
* @num1: num1 argument has value for number1
* @num2: num2 argument has value for number2
* Return: division of two integers
*
*/
int div(int num1, int num2)
{
if (num2 == 0)
return (1);
return (num1 / num2);
}
