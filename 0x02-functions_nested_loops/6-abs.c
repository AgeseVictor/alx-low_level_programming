#include "main.h"
/**
 *  * _abs - absolute value
 *   * @n: integer
 *    *
 *     * Return: return absolute valuer of a number
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
